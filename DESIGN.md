# DESIGN.md

## 이게 뭐냐

"지금 뛰어야 하나?" — 늦었을 때 목적지 역만 입력하면 현재 화면 하나에서
**가장 가까운 역까지 도보 시간**과 **타야 할 다음 열차의 실시간 도착**을
비교해 뛸지 말지 판정해 주는 모바일 우선 원페이지 웹앱. 로그인 없음,
DB 없음. 판정이 화면의 유일한 주인공이고 나머지는 그 판정을 뒷받침하는
근거(가까운 역, 경로, 도착 목록)로 아래에 쌓인다.

현재는 기능 구현 위주로 만든 **1차 스타일**(중립 그레이 팔레트 + 시스템
산세리프)이라 톤·개성이 약하다. 이 문서는 리디자인을 위한 현재 상태
기준선(baseline)이다.

## 화면 구조

전체가 세로 스크롤 한 화면. 좌우 여백 16px, 최대 폭 `max-w-md`(448px)로
고정 — 데스크톱 브라우저에서도 폭이 안 늘어나고 중앙에 좁게 떠 있다.
별도 브레이크포인트 없음(모바일 레이아웃 그대로 확대).

위에서 아래로:

1. **헤더** — 타이틀 + 서브카피 + (우측) 테마 토글
2. **목적지 입력**
3. **위치 상태 한 줄** (또는 위치 실패 시 출발역 수동 선택 카드)
4. **결과 영역** (목적지 선택 후에만 등장)
   1. 판정 카드 — 화면에서 가장 크고 색이 강한 요소
   2. 경로 요약 카드 — 가까운 역·도보시간, 구간(호선/방면), 환승·소요
   3. "OO역 다음 열차" 리스트
   4. (있으면) 에러/안내 텍스트
5. **푸터** — 실시간/목업 배지, 도보시간 추정 고지

## 상태별 화면

| 상태 | 화면 |
| --- | --- |
| 최초 진입 | 헤더 + 빈 목적지 입력 + "현위치 확인 중…" |
| 위치 확인됨, 목적지 미선택 | "현위치 확인됨 · 가까운 역 **OO**" 한 줄만 |
| 위치 거부/실패 | 회색 카드: 안내문 + "다시 시도" 버튼 + 출발역 검색 입력 + 도보 시간 슬라이더(1~15분) |
| 목적지 선택, 도착 정보 로딩 중 | 판정 카드 자리에 "도착 정보 불러오는 중…" 플레이스홀더 |
| 결과 있음 | 판정 카드 + 경로 요약 + 다음 열차 리스트 |
| 경로 못 찾음 | 카드 없이 안내 텍스트 한 줄만 |
| 도착 정보 없음 | 판정 카드 자체가 "도착 정보 없음 / 전광판을 확인하세요" 중립 문구로 대체 |

## 컴포넌트

### 헤더
타이틀 "지금 뛰어야 하나?"(`text-xl font-extrabold`) + 서브카피
"목적지만 넣으면 뛸지 말지 알려드려요"(`text-xs text-dim`), 오른쪽에
테마 토글 버튼(텍스트만, 테두리 있는 작은 필).

### DestinationInput (목적지 · 출발역 겸용)
두 가지 뷰를 토글:
- **빈 상태**: placeholder 입력창 → 타이핑하면 로컬 자동완성 드롭다운
  (역 이름 + 랜드마크 별칭 매칭, 노선 목록 우측 표시). 키보드로 위/아래/엔터/esc
  조작 가능, 바깥 클릭 시 닫힘.
- **선택됨 상태**: 라벨(작게) + 선택한 역(크게, 굵게) + 우측 "변경" 버튼.

같은 컴포넌트를 `label`/`placeholder`만 바꿔 출발역 수동 선택에도 재사용.

### 위치 상태 / ManualOrigin
평상시엔 dim 텍스트 한 줄. 위치 거부·실패 시에만 카드로 전환되어
DestinationInput(출발역용) + range 슬라이더를 보여줌.

### VerdictCard — 화면의 핵심
판정 종류(run/missed/hurry/relax)에 따라 배경·글자·테두리 색이 통째로
바뀌는 큰 카드(rounded-2xl, padding 20px):
- 좌측 큰 이모지 아이콘(`text-3xl`): 🏃 / 😮‍💨 / 🚶‍♂️💨 / 🚶
- 헤드라인(`text-2xl font-extrabold`) — 화면에서 가장 큰 텍스트
- 한 줄 설명(`text-sm`)
- `run` 상태에서만 `pulse-ring` 애니메이션(카드 테두리 밖으로 퍼졌다
  사라지는 ring, 1.4s 무한 반복)으로 긴급함을 강조

### RouteSummary
흰 카드(`rounded-2xl border`). 상단에 "가장 가까운 역 + 도보 N분 · 거리"
한 줄(우측 정렬 값), 그 아래 구간 목록(호선 배지 `bg-surface-2` pill +
"출발역 → 도착역" + 방면 텍스트), 맨 아래 환승/총 소요/총 역수를 작은
`text-faint` 한 줄로 요약.

### ArrivalList
카드형 리스트, 최대 5개. 각 항목: 좌측에 호선·방면·원문 메시지, 우측에
남은 시간을 큰 숫자(`tabular-nums font-bold`)로 — 탑승 가능하면 relax색,
아니면 run색으로 미리 신호를 줌. 내가 탈 방면이 아닌 열차는 카드 전체가
반투명(`opacity-70`) + "다른 방면" 회색 배지로 시각적 우선순위를 낮춤.

### 푸터
중앙 정렬, `text-[11px] text-faint`. 목업 모드면 pill 배지로
"목업 데이터 · 실시간 아님", 아니면 "실시간 도착: 서울시 열린데이터광장".
그 아래 도보시간이 추정치라는 고지 한 줄.

## 색

라이트가 기본, `<html>.dark` 클래스로 전환(테마 토글이 `localStorage`에
저장, 첫 페인트 전 스크립트로 FOUC 방지). 지금은 브랜드색 없는 중립
그레이 + 파랑 accent 1개뿐이고, 판정 3색만 톤이 있다.

| 용도 | 라이트 | 다크 |
| --- | --- | --- |
| 페이지 배경 `--bg` | `#F4F5F7` | `#101216` |
| 카드 배경 `--surface` | `#FFFFFF` | `#191C22` |
| 보조 배경 `--surface-2` | `#F0F1F4` | `#21242C` |
| 테두리 `--border` | `#E2E4E9` | `#2C303A` |
| 본문 글자 `--fg` | `#1C1E23` | `#E7E9EE` |
| 보조 글자 `--dim` | `#6B7280` | `#9AA1AC` |
| 옅은 글자 `--faint` | `#9AA1AC` | `#6B7280` |
| 액센트(포커스 등) `--accent` | `#2563EB` | `#5B8BF7` |
| 판정: 뛰어(run) | bg `#FDECEC` / fg `#C02626` / line `#F2B8B8` | bg `#2C1618` / fg `#F0908F` / line `#5A2A2C` |
| 판정: 빠르게(hurry) | bg `#FDF2E0` / fg `#B5710B` / line `#F0D5A3` | bg `#2B2013` / fg `#E6B366` / line `#5A4527` |
| 판정: 여유(relax) | bg `#E9F7EE` / fg `#1F8A4C` / line `#B4E2C6` | bg `#14261C` / fg `#7BD6A0` / line `#2C4D3A` |

`missed`(아슬아슬) 상태는 별도 팔레트 없이 `run`과 같은 색을 재사용 중.

## 타이포그래피

전부 시스템 산세리프 1종: `ui-sans-serif, system-ui, -apple-system,
Apple SD Gothic Neo, Malgun Gothic, Segoe UI, Roboto`. 커스텀 웹폰트
없음. 크기 위계가 얕음 — 대부분 `text-sm`/`text-xs`이고, 판정 헤드라인
(`text-2xl`)과 선택된 역 이름(`text-lg`)만 예외적으로 크다.

## 인터랙션 · 모션

- 15초마다 도착 정보 재조회 + 1초 카운트다운(남은 시간 실시간 감소),
  탭이 백그라운드면 정지. 푸터 근처에 "n초 전" 신선도 표시
- `run` 판정에만 pulse-ring 애니메이션
- 자동완성 드롭다운: hover 시 항목 배경, 키보드 포커스 이동
- 링크/버튼 트랜지션은 색상 전환(`transition-colors`) 정도만 사용
- `prefers-reduced-motion: reduce` 시 모든 애니메이션·트랜지션 정지

## 접근성

`role="combobox"` + `aria-expanded`/`aria-controls`/`aria-autocomplete`
(자동완성), `role="listbox"`/`"option"` + `aria-selected`, 테마 버튼
`aria-label`, 이모지 아이콘에 `aria-hidden`.

## 구조 (파일 위치)

```
src/app/page.tsx                 화면 전체 (섹션 배치 + 상태 분기)
src/app/globals.css              색 토큰(:root/.dark), 폰트, pulse 애니메이션
src/components/
  DestinationInput.tsx           목적지·출발역 자동완성 입력
  VerdictCard.tsx                판정 카드 (색 전환의 중심)
  ArrivalList.tsx                다음 열차 리스트
  ThemeToggle.tsx                라이트/다크 토글
```

리디자인 시 색 토큰은 `globals.css`의 `:root`/`.dark`만 바꾸면 전체
화면에 반영된다(컴포넌트들이 전부 `var(--*)`/Tailwind 유틸을 씀). 이
문서를 갱신하지 않고 디자인만 바꾸면 곧 사실과 어긋나니, 색·레이아웃을
바꿀 때 이 파일도 같이 고칠 것.
